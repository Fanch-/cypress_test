language: node_js
node_js:
  - 10
addons:
  apt:
    packages:
      # Ubuntu 16+ does not install this dependency by default, so we need to install it ourselves
      - libgconf-2-4
cache:
  # Caches $HOME/.npm when npm ci is default script command
  # Caches node_modules in all other cases
  npm: true
  directories:
    # we also need to cache folder with Cypress binary
    - ~/.cache
install:
  - npm ci
#env:
#  - TESTFOLDER=/home/travis/build/Fanch-/cypress_test/cypress/integration/examples
#  - TESTFOLDER=/home/travis/build/Fanch-/cypress_test/cypress/integration/examples2
script:
   - cypress run --record --key 57c262aa-21c8-4078-9dbd-0a9ea1eeaad2 --parallel --group 2x-electron
